###############################################
# Subsetting
###############################################
## Clear variables:
rm(list = ls(envir = environment()))
val <- outer(1:5, 1:5, FUN = 'paste')
